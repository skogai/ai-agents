# [Project name]

[One-line summary of what this project does and who it is for.]

## Why

[State the problem this project solves in two or three sentences. Name the audience and the pain. If a reader cannot tell within ten seconds whether this project is for them, the summary is too vague.]

## Quick Start

A new contributor should be able to clone, install, and run the project in under five minutes.

```bash
# 1. Clone
git clone [repo URL]
cd [project directory]

# 2. Install
[install command]

# 3. Run
[run command]
```

You should see [expected first-run output or behavior].

## Project Structure

```text
[project-root]/
|-- [src or app dir]/    [brief role]
|-- [tests dir]/         [brief role]
|-- [docs dir]/          [brief role]
`-- [config dir]/        [brief role]
```

## Common Tasks

| Task | Command |
|------|---------|
| Run tests | `[test command]` |
| Lint | `[lint command]` |
| Build | `[build command]` |
| Start dev server | `[dev command]` |
| Format code | `[format command]` |

## Configuration

| Variable | Purpose | Default |
|----------|---------|---------|
| `[ENV_VAR_ONE]` | [What it controls] | `[default]` |
| `[ENV_VAR_TWO]` | [What it controls] | `[default]` |

Place secrets in `.env` (never commit). See `.env.example` for the full list.

## Troubleshooting

| Symptom | Cause | Fix |
|---------|-------|-----|
| `[error message]` | [Likely cause] | [Concrete fix] |
| `[error message]` | [Likely cause] | [Concrete fix] |
| `[error message]` | [Likely cause] | [Concrete fix] |

## Contributing

1. Read `[contributing guide path]` for the full workflow.
2. Open an issue before substantial changes so reviewers can weigh in early.
3. Match the existing style; the linter and formatter are the source of truth.
4. Add tests with every behavior change.

## License

[License name]. See `LICENSE` for the full text.
